'use strict';
import f from '@';

describe('COUPDAYBS', () => {
	it('is a function', () => {
		expect(f.COUPDAYS).toBeInstanceOf(Function);
	});
});

describe('COUPDAYS', () => {
	it('is a function', () => {
		expect(f.COUPDAYS).toBeInstanceOf(Function);
	});
});

describe('COUPDAYSNC', () => {
	it('is a function', () => {
		expect(f.COUPDAYSNC).toBeInstanceOf(Function);
	});
});

describe('COUPNCD', () => {
	it('is a function', () => {
		expect(f.COUPNCD).toBeInstanceOf(Function);
	});
});

describe('COUPNUM', () => {
	it('is a function', () => {
		expect(f.COUPNUM).toBeInstanceOf(Function);
	});
});

describe('COUPPCD', () => {
	it('is a function', () => {
		expect(f.COUPPCD).toBeInstanceOf(Function);
	});
});
